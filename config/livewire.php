<?php

return [
    // Reverting to default or empty config if needed, but actually I'll just leave it minimal or empty to rely on defaults.
    // Or better, I can just delete it via write_file with empty content if I can't delete it.
    // But since I can't delete, I'll just comment out the change.
    /*
    'temporary_file_upload' => [
        'disk' => 'local',
        'directory' => 'livewire-tmp',
    ],
    */
];
